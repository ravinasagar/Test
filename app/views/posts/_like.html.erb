<%if  post.likes.where(user_id: $current_user.id).present? %>
  <%= link_to 'Unlike',  like_post_path(post.id), method: :post, remote: true, class: 'btn btn-primary btn-lg btn-block' %>
<% else%>
  <%= link_to 'Like',like_post_path(post.id), method: :post, remote: true, class: 'btn btn-primary btn-lg btn-block' %>
  <% end %>
<%=post.likes.count%>
