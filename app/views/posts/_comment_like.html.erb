<%#binding.pry%>
<%if  comment.likes.where(user_id: $current_user.id).present? %>
  <%= link_to 'Unlike', comment_like_post_path(comment.id), method: :post, remote: true, class: 'btn btn-primary btn-lg btn-block' %>
<% else%>
  <%= link_to 'Like',comment_like_post_path(comment.id), method: :post, remote: true, class: 'btn btn-primary btn-lg btn-block' %>
  <% end %>
  <%=comment.likes.count%>

