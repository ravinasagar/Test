<%= form_with(model: registration, local: true) do |form| %>
  <% if registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
      <% registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_name %>
    <%= form.text_field :student_name, id: :registration_student_name %>
  </div>

  <div class="field">
    <%= form.label :course %>
    <%= form.text_field :course, id: :registration_course %>
  </div>

  <div class="field">
    <%= form.label :new %>
    <%= form.text_field :new, id: :registration_new %>
  </div>

  <div class="field">
    <%= form.label :cart_id %>
    <%= form.number_field :cart_id, id: :registration_cart_id %>
  </div>

  <div class="field">
    <%= form.label :ip_address %>
    <%= form.text_field :ip_address, id: :registration_ip_address %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :registration_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :registration_last_name %>
  </div>

  <div class="field">
    <%= form.label :card_type %>
    <%= form.text_field :card_type, id: :registration_card_type %>
  </div>

  <div class="field">
    <%= form.label :card_expires_on %>
    <%= form.date_select :card_expires_on, id: :registration_card_expires_on %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
